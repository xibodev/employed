# Pinned for reproducible builds (EMP-021).
fastapi==0.135.2
uvicorn[standard]==0.42.0
python-jose[cryptography]==3.3.0
bcrypt==5.0.0
python-slugify==8.0.4
bleach==6.3.0
httpx==0.28.1
python-multipart==0.0.20
# Required by Starlette's templating module, which sentry-sdk's Starlette/FastAPI
# integration imports during setup_once(). Without it, enabling SENTRY_DSN crashes
# the app at startup ("jinja2 must be installed to use Jinja2Templates").
jinja2==3.1.4
# HTML->PDF engine for server-side resume rendering (R14). Pure-Python
# (reportlab/html5lib) — needs no system libraries on python:3.12-slim. The
# renderer in app/services/resume_templates.py prefers WeasyPrint if present,
# then xhtml2pdf, then a built-in text fallback; this pins the working engine.
xhtml2pdf==0.2.16
# S3-compatible client for durable resume-artifact storage on Cloudflare R2
# (app/services/resume_storage.py). Range matches the portfolio convention.
boto3>=1.34,<2.0

# Pinned for reproducible builds (EMP-021). Versions match the suite-green
# local environment; bump deliberately, not implicitly.
sqlalchemy[asyncio]==2.0.49
alembic==1.18.4
psycopg2-binary==2.9.10
pydantic-settings==2.14.0
python-dotenv==1.0.0
email-validator==2.1.0
sentry-sdk[fastapi]==2.18.0

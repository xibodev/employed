# Canonical manifest of the SSM SecureString parameters the deploy pipeline MUST
# have populated (non-empty, no PLACEHOLDER) under /employed/prod/ BEFORE a prod
# deploy. Mirrors Kumbuka's REQUIRED_SECURESTRING_PARAMS pattern. The pre-deploy
# validation in .github/workflows/deploy-prod.yml reads this list and fails the
# deploy if any value is missing/empty/placeholder.
#
# Secret VALUES are never created in IaC — they are seeded out-of-band (operator
# / pipeline) as SecureStrings (free), and referenced by name at runtime via
# render-env.sh. Lines starting with '#' and blank lines are ignored.
#
# --- database ---
db-master-password
DATABASE_URL
# --- app core ---
SECRET_KEY
IP_SALT
# --- payments (Stripe) ---
STRIPE_SECRET_KEY
STRIPE_PUBLISHABLE_KEY
STRIPE_WEBHOOK_SECRET
# --- auth (Google OAuth) ---
GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET
# --- bot protection (reCAPTCHA) ---
NEXT_PUBLIC_RECAPTCHA_SITE_KEY
RECAPTCHA_SECRET_KEY
# --- email (SES SMTP) ---
SMTP_USERNAME
SMTP_PASSWORD
# --- ingress (Cloudflare tunnel) ---
CLOUDFLARED_TOKEN
# --- observability (Bugsink error tracking, Sentry-SDK compatible) ---
SENTRY_DSN
